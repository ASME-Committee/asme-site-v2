/**
 * Removed. Nothing imports this any more.
 *
 * SpotlightCard added a cursor-tracking radial gradient behind a card, tinted
 * blue on even cards and coral on odd ones. On a page of white panels with no
 * card borders that read as two different card types alternating down a grid,
 * and the coral was the last survivor of the previous palette. Every card that
 * used it now has one solid hover instead: the icon chip fills with the accent
 * and the glyph knocks out to white.
 *
 * The file itself is a tombstone. The running dev server keeps a deleted module
 * in its graph and fails to compile until it restarts, so this stays on disk
 * until the next restart, at which point it can go for good.
 */
export {};
